# Chatbot

**A riga `16` dovete specificare il percorso del modello che volete ulitizzare, per ora sto utilizzando `llama.cpp`, che runna il modello localmente. Per runnarlo avete bisogno di almeno ~4-15GB (in base alla grandezza del modello) di RAM/VRAM**

Lista di alcuni modelli: https://huggingface.co/models?search=gguf

```bash
python main.py
```
